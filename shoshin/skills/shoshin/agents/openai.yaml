interface:
  display_name: "Shoshin"
  short_description: "Route engineering goals to the necessary methods"
  default_prompt: "Use $shoshin to select the necessary workflow for this goal, preserving the requested scope and stopping point."
policy:
  allow_implicit_invocation: true
