interface:
  display_name: "Grill Me"
  short_description: "Sharpen a plan through interview"
  default_prompt: "Use $grill-me to stress-test my design with the upstream interview workflow."
policy:
  allow_implicit_invocation: true
